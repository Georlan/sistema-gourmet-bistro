name: Koma Quality Gate

on:
  workflow_dispatch:
  schedule:
    - cron: '23 5 * * *'
  pull_request:
    branches: [main]

concurrency:
  group: quality-gate-${{ github.event.pull_request.head.ref || github.ref_name || github.event_name }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  plan:
    name: Plan fast PR gates
    runs-on: ubuntu-latest
    timeout-minutes: 3
    outputs:
      frontend: ${{ steps.classify.outputs.frontend }}
      backend: ${{ steps.classify.outputs.backend }}
      security: ${{ steps.classify.outputs.security }}
      e2e: ${{ steps.classify.outputs.e2e }}
      idempotency: ${{ steps.classify.outputs.idempotency }}
    steps:
      - name: Checkout history for change classification
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Classify changed paths
        id: classify
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail

          frontend=false
          backend=false
          security=false
          e2e=false
          idempotency=false

          # Manual/nightly runs are the complete regression safety net.
          if [[ "$EVENT_NAME" != "pull_request" ]]; then
            frontend=true
            backend=true
            security=true
            e2e=false
            idempotency=false
          else
            git diff --name-only "$BASE_SHA" "$HEAD_SHA" | tee /tmp/koma-changed-files.txt

            while IFS= read -r file; do
              [[ -z "$file" ]] && continue

              case "$file" in
                src/*|tests/*.test.ts|public/*|package.json|package-lock.json|tsconfig*.json|vite.config.*)
                  frontend=true
                  ;;
              esac

              case "$file" in
                backend/*)
                  backend=true
                  ;;
              esac

              case "$file" in
                .env.example|backend/alembic/*|backend/app/database.py|backend/app/models.py|backend/app/security.py|backend/app/main.py|backend/app/routes/auth*|backend/app/routes/super_admin*|backend/app/routes/online_payments.py|backend/app/services/online_payments/*|backend/app/services/customer_auth.py|backend/tests/test_*security*|backend/tests/test_*postgres*|backend/tests/test_multitenant_infra.py|backend/tests/test_super_admin_contract.py|backend/tests/test_cors_and_security_headers.py|backend/tests/test_cardapio_public_cors_railway.py)
                  security=true
                  ;;
              esac

              case "$file" in
                e2e/*|playwright.config.*|.github/workflows/e2e-pr-gate.yml|.github/workflows/quality-gate.yml)
                  e2e=true
                  ;;
              esac

              case "$file" in
                backend/app/application/orders/idempotency.py|backend/app/application/orders/service.py|backend/app/adapters/orders/web_adapter.py|backend/app/models.py|backend/alembic/versions/*idempotency*|backend/tests/test_order_idempotency_fingerprint.py|.github/workflows/quality-gate.yml)
                  idempotency=true
                  ;;
              esac
            done < /tmp/koma-changed-files.txt
          fi

          {
            echo "frontend=$frontend"
            echo "backend=$backend"
            echo "security=$security"
            echo "e2e=$e2e"
            echo "idempotency=$idempotency"
          } | tee -a "$GITHUB_OUTPUT"

  frontend:
    name: Frontend typecheck + unit + build
    needs: plan
    if: needs.plan.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: TypeScript check
        run: npm run lint
      - name: Frontend unit tests
        run: npm run test:unit
      - name: Production build
        run: npm run build

  backend-pr:
    name: Backend critical smoke
    needs: plan
    if: github.event_name == 'pull_request' && needs.plan.outputs.backend == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      ENVIRONMENT: test
      SECRET_KEY: quality-gate-secret-key-2026-only-for-ci
      ENCRYPTION_KEY: MDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDA=
      PYTHONPATH: backend
      BASE_SHA: ${{ github.event.pull_request.base.sha }}
      HEAD_SHA: ${{ github.event.pull_request.head.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13.15'
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt
      - name: Install backend dependencies
        run: python -m pip install -r backend/requirements-dev.txt
      - name: Verify installed dependency compatibility
        run: python -m pip check
      - name: Reject tests outside canonical suite
        run: |
          if find backend -maxdepth 1 -type f -name 'test_*.py' -print -quit | grep -q .; then
            find backend -maxdepth 1 -type f -name 'test_*.py' -print
            exit 1
          fi
      - name: Validate Alembic single head
        working-directory: backend
        run: |
          heads="$(python -m alembic -c alembic.ini heads)"
          printf '%s\n' "$heads"
          count="$(printf '%s\n' "$heads" | grep -c '(head)')"
          test "$count" -eq 1
      - name: Run changed tests plus critical smoke
        shell: bash
        run: |
          set -euo pipefail
          baseline=(
            backend/tests/test_critical_auth_flow.py
            backend/tests/test_online_order_robustness.py
            backend/tests/test_order_numbering_unified.py
            backend/tests/test_websocket_credentials.py
          )
          selected=("${baseline[@]}")

          while IFS= read -r file; do
            [[ -f "$file" ]] && selected+=("$file")
          done < <(git diff --name-only "$BASE_SHA" "$HEAD_SHA" -- 'backend/tests/test_*.py')

          declare -A seen=()
          unique=()
          for file in "${selected[@]}"; do
            if [[ -z "${seen[$file]:-}" ]]; then
              unique+=("$file")
              seen[$file]=1
            fi
          done

          printf 'Fast backend gate: %s test files\n' "${#unique[@]}"
          printf '  %s\n' "${unique[@]}"
          python -m pytest "${unique[@]}" -q -ra -x

  browser-pr:
    name: Browser regression for E2E changes
    needs: plan
    if: github.event_name == 'pull_request' && needs.plan.outputs.e2e == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    env:
      BASE_SHA: ${{ github.event.pull_request.base.sha }}
      HEAD_SHA: ${{ github.event.pull_request.head.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Run browser matrix
        env:
          KOMA_E2E_PORT: '4283'
        shell: bash
        run: |
          set -euo pipefail

          canonical_specs=(
            e2e/operational-app-owners.spec.ts
            e2e/salon-context.spec.ts
            e2e/app-recovery.spec.ts
            e2e/public-menu-order.spec.ts
            e2e/order-tracking-chat.spec.ts
            e2e/password-recovery.spec.ts
          )

          mapfile -t changed_specs < <(
            git diff --diff-filter=ACMR --name-only "${BASE_SHA:-origin/${{ github.base_ref }}}...${HEAD_SHA:-HEAD}" -- 'e2e/*.spec.ts'
          )

          specs=("${canonical_specs[@]}")
          for changed_spec in "${changed_specs[@]}"; do
            if [[ -n "${changed_spec}" ]] && [[ -f "${changed_spec}" ]] && [[ ! " ${specs[*]} " =~ " ${changed_spec} " ]]; then
              specs+=("${changed_spec}")
            fi
          done

          printf 'Playwright specs: %s\n' "${specs[*]}"
          npx playwright test "${specs[@]}" --project=mobile-360 --project=desktop-1366 --workers=2
      - name: Upload browser diagnostics
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: e2e-pr-playwright-report-${{ github.run_id }}
          path: playwright-report/
          if-no-files-found: warn
          retention-days: 7

  browser-full:
    name: Browser full regression
    needs: plan
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Run browser matrix
        env:
          KOMA_E2E_PORT: '4283'
        run: npm run test:e2e -- e2e/operational-app-owners.spec.ts e2e/salon-context.spec.ts e2e/app-recovery.spec.ts e2e/public-menu-order.spec.ts e2e/order-tracking-chat.spec.ts e2e/password-recovery.spec.ts --project=mobile-360 --project=desktop-1366 --workers=2
      - name: Upload browser diagnostics
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: playwright-report-${{ github.run_id }}
          path: playwright-report/
          if-no-files-found: warn
          retention-days: 7

  backend-full:
    name: Backend full shard ${{ matrix.shard }} of 4
    needs: plan
    if: github.event_name != 'pull_request' && needs.plan.outputs.backend == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]
    env:
      ENVIRONMENT: test
      SECRET_KEY: quality-gate-secret-key-2026-only-for-ci
      ENCRYPTION_KEY: MDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDA=
      PYTHONPATH: backend
      SHARD_TOTAL: '4'
      SHARD_INDEX: ${{ matrix.shard }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13.15'
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt
      - name: Install backend dependencies
        run: python -m pip install -r backend/requirements-dev.txt
      - name: Reject tests outside canonical suite
        run: |
          if find backend -maxdepth 1 -type f -name 'test_*.py' -print -quit | grep -q .; then
            find backend -maxdepth 1 -type f -name 'test_*.py' -print
            exit 1
          fi
      - name: Validate Alembic single head
        working-directory: backend
        run: |
          heads="$(python -m alembic -c alembic.ini heads)"
          printf '%s\n' "$heads"
          count="$(printf '%s\n' "$heads" | grep -c '(head)')"
          test "$count" -eq 1
      - name: Run backend shard
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t files < <(find backend/tests -maxdepth 1 -type f -name 'test_*.py' | sort)
          selected=()
          zero_based=$((SHARD_INDEX - 1))

          for i in "${!files[@]}"; do
            if (( i % SHARD_TOTAL == zero_based )); then
              selected+=("${files[$i]}")
            fi
          done

          if (( ${#selected[@]} == 0 )); then
            echo "No tests selected for shard $SHARD_INDEX/$SHARD_TOTAL"
            exit 1
          fi

          printf 'Shard %s/%s: %s test files\n' "$SHARD_INDEX" "$SHARD_TOTAL" "${#selected[@]}"
          python -m pytest "${selected[@]}" -q -ra

  security:
    name: postgres-security-audit
    needs: plan
    if: needs.plan.outputs.security == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: koma_security_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d koma_security_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      ENVIRONMENT: test
      SECRET_KEY: security-audit-secret-key-2026-only-for-ci
      ENCRYPTION_KEY: MDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDA=
      PYTHONPATH: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13.15'
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt
      - name: Install backend dependencies
        run: python -m pip install -r backend/requirements-dev.txt
      - name: Run security regression gate
        run: >-
          python -m pytest
          backend/tests/test_cors_and_security_headers.py
          backend/tests/test_cardapio_public_cors_railway.py
          backend/tests/test_super_admin_contract.py
          backend/tests/test_multitenant_infra.py
          -q -ra -x
      - name: Bootstrap Supabase compatibility roles
        env:
          PG_ADMIN_URL: postgresql://postgres:postgres@localhost:5432/koma_security_test
        run: >-
          python -c "import os, psycopg2;
          c=psycopg2.connect(os.environ['PG_ADMIN_URL']); c.autocommit=True;
          q=c.cursor();
          q.execute('CREATE ROLE anon NOLOGIN');
          q.execute('CREATE ROLE authenticated NOLOGIN');
          q.execute('CREATE ROLE service_role NOLOGIN');
          q.close(); c.close()"
      - name: Apply schema to ephemeral PostgreSQL
        working-directory: backend
        env:
          DATABASE_URL: postgresql+psycopg2://postgres:postgres@localhost:5432/koma_security_test
          MIGRATION_DATABASE_URL: postgresql+psycopg2://postgres:postgres@localhost:5432/koma_security_test
        run: python -m alembic -c alembic.ini upgrade head
      - name: Provision restricted runtime role
        env:
          PG_ADMIN_URL: postgresql://postgres:postgres@localhost:5432/koma_security_test
        run: >-
          python -c "import os, psycopg2;
          c=psycopg2.connect(os.environ['PG_ADMIN_URL']); c.autocommit=True;
          q=c.cursor();
          q.execute(\"CREATE ROLE koma_security_runtime LOGIN PASSWORD 'koma-security-runtime' IN ROLE koma_app\");
          q.close(); c.close()"
      - name: Run real PostgreSQL operational and RLS regressions
        env:
          DATABASE_URL: postgresql+psycopg2://koma_security_runtime:koma-security-runtime@localhost:5432/koma_security_test
          MIGRATION_DATABASE_URL: postgresql+psycopg2://postgres:postgres@localhost:5432/koma_security_test
          KOMA_PYTEST_USE_EXTERNAL_DATABASE: "true"
          STRICT_RLS_ROLE_CHECK: "true"
          CREATE_SCHEMA_ON_STARTUP: "false"
          KOMA_CONCURRENCY_DATABASE_URL: postgresql+psycopg2://koma_security_runtime:koma-security-runtime@localhost:5432/koma_security_test
        run: >-
          python -m pytest
          backend/tests/test_super_admin_incidents_postgres.py
          backend/tests/test_order_chat_postgres_rls.py
          backend/tests/test_password_recovery_postgres.py
          backend/tests/test_signup_postgres.py
          backend/tests/test_b14_operational_smoke_postgres.py
          backend/tests/test_atendimento_concurrency_postgres.py
          backend/tests/test_first_client_payment_simulation.py
          -q -ra -x

  postgres-idempotency:
    name: postgres-idempotency
    needs: plan
    if: github.event_name == 'pull_request' && needs.plan.outputs.idempotency == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: koma_idempotency_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d koma_idempotency_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      ENVIRONMENT: test
      SECRET_KEY: quality-gate-secret-key-2026-only-for-ci
      ENCRYPTION_KEY: MDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDA=
      PYTHONPATH: backend
      DATABASE_URL: postgresql+psycopg2://postgres:postgres@localhost:5432/koma_idempotency_test
      MIGRATION_DATABASE_URL: postgresql+psycopg2://postgres:postgres@localhost:5432/koma_idempotency_test
      KOMA_CONCURRENCY_DATABASE_URL: postgresql+psycopg2://postgres:postgres@localhost:5432/koma_idempotency_test
      STRICT_RLS_ROLE_CHECK: "false"
      CREATE_SCHEMA_ON_STARTUP: "false"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13.15'
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt
      - name: Install backend dependencies
        run: python -m pip install -r backend/requirements-dev.txt
      - name: Bootstrap Supabase compatibility roles
        env:
          PG_ADMIN_URL: postgresql://postgres:postgres@localhost:5432/koma_idempotency_test
        run: >-
          python -c "import os, psycopg2;
          c=psycopg2.connect(os.environ['PG_ADMIN_URL']); c.autocommit=True;
          q=c.cursor();
          q.execute('CREATE ROLE anon NOLOGIN');
          q.execute('CREATE ROLE authenticated NOLOGIN');
          q.execute('CREATE ROLE service_role NOLOGIN');
          q.close(); c.close()"
      - name: Apply schema to ephemeral PostgreSQL
        working-directory: backend
        run: python -m alembic -c alembic.ini upgrade head
      - name: Run idempotency concurrency and fingerprint regressions
        run: >-
          python -m pytest
          backend/tests/test_order_idempotency_fingerprint.py
          -q -ra -v

  verdict:
    name: Merge verdict
    if: always()
    needs: [plan, frontend, backend-pr, browser-pr, browser-full, backend-full, security, postgres-idempotency]
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Require selected fast PR lanes to pass
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          PLAN_RESULT: ${{ needs.plan.result }}
          FRONTEND_RESULT: ${{ needs.frontend.result }}
          BACKEND_PR_RESULT: ${{ needs.backend-pr.result }}
          BROWSER_PR_RESULT: ${{ needs.browser-pr.result }}
          BROWSER_RESULT: ${{ needs.browser-full.result }}
          BACKEND_FULL_RESULT: ${{ needs.backend-full.result }}
          SECURITY_RESULT: ${{ needs.security.result }}
          IDEMPOTENCY_RESULT: ${{ needs.postgres-idempotency.result }}
        run: |
          set -euo pipefail

          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            results=("$PLAN_RESULT" "$FRONTEND_RESULT" "$BACKEND_PR_RESULT" "$BROWSER_PR_RESULT" "$SECURITY_RESULT" "$IDEMPOTENCY_RESULT")
          else
            results=("$PLAN_RESULT" "$FRONTEND_RESULT" "$BROWSER_RESULT" "$BACKEND_FULL_RESULT" "$SECURITY_RESULT")
          fi

          for result in "${results[@]}"; do
            case "$result" in
              success|skipped) ;;
              *) echo "Quality gate lane finished as: $result"; exit 1 ;;
            esac
          done

          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            echo "Fast PR gate passed. Heavy browser/backend regressions run nightly or manually."
          else
            echo "Full regression gate passed."
          fi
